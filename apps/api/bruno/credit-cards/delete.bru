meta {
  name: delete credit card
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/api/v1/credit-cards/{{creditCardId}}
}

headers {
  Cookie: {{authCookie}}
}

tests {
  test("credit card delete returns 204", function() {
    expect(res.status).to.equal(204);
  });
}
