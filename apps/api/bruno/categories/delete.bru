meta {
  name: delete category
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/categories/{{categoryId}}
  body: none
  auth: none
}

headers {
  Cookie: {{authCookie}}
}

tests {
  test("category delete returns 204", function() {
    expect(res.status).to.equal(204);
  });
}
