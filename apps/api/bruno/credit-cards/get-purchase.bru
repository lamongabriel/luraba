meta {
  name: get purchase
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/credit-cards/{{creditCardId}}/purchases/{{creditCardPurchaseId}}
}

headers {
  Cookie: {{authCookie}}
}

tests {
  test("credit card purchase returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
