meta {
  name: delete payment
  type: http
  seq: 17
}

delete {
  url: {{baseUrl}}/api/v1/credit-cards/{{creditCardId}}/payments/{{creditCardPaymentId}}
}

headers {
  Cookie: {{authCookie}}
}

tests {
  test("credit card payment delete returns 204", function() {
    expect(res.status).to.equal(204);
  });
}
