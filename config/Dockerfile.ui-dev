FROM node:22-alpine

RUN corepack enable && corepack prepare pnpm@10.28.2 --activate
WORKDIR /app

COPY ui/package.json ui/pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile --ignore-scripts

COPY config/check-ui.mjs /opt/luraba/check-ui.mjs
EXPOSE 3000
CMD ["pnpm", "dev"]
