meta {
  name: get my preferences
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/auth/me/preferences
}

headers {
  Cookie: {{authCookie}}
}

tests {
  test("preferences returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
