meta {
  name: revoke invite
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/api/v1/households/{{householdId}}/invites/{{householdInviteId}}
}

headers {
  Cookie: {{authCookie}}
}

tests {
  test("invite revoke returns 204", function() {
    expect(res.status).to.equal(204);
  });
}
