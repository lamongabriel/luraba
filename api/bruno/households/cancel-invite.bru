meta {
  name: cancel invite
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/api/v1/households/{{householdId}}/invites/{{householdInviteId}}
}

headers {
  Cookie: {{authCookie}}
}

tests {
  test("invite cancel returns 204", function() {
    expect(res.status).to.equal(204);
  });
}
