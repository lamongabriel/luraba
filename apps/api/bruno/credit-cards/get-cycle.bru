meta {
  name: get cycle
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/credit-cards/{{creditCardId}}/cycles/{{creditCardCycleId}}
}

headers {
  Cookie: {{authCookie}}
}

tests {
  test("credit card cycle returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
