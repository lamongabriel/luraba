meta {
  name: list accounts
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/accounts
}

headers {
  Cookie: {{authCookie}}
}

tests {
  test("list accounts returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
